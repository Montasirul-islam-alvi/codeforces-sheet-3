#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    long long int n,i,sum = 0;
    cin>>n;
    int A[n];
    for(i=0;i<n;i++)
    {
        cin>>A[i];
        sum+=A[i];
    }
    cout<<abs(sum);
}
